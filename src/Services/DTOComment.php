<?php

// namespace App\DTO;

// // use App\Entity\.....;

// class NameDto
// {
//     // Constructeur avec toutes les propriétés nécessaires 
//     public function __construct(
//         public int $id,
//         public string $name,
        
//     ) {
//     }

//     // Méthode statique pour créer une instance de BorneDto à partir d'une entité ChargingPoint
//     /**
//      * En params name entity
//      *
//      * @return self
//      */
//     public static function create(): self
//     {
        // Extraction des données de l'adresse associée à la borne exemple
        // $adress = $chargingPoint->getAdressId();
        // $adressId = $adress ? $adress->getId() : null;
        // Extraction des données des disponibilités de la borne
        // $availabilities = $chargingPoint->getAvailabilities();
        // $availability = $availabilities->first();

        // Formatage des dates et des heures pour la sérialisation en JSON
        // $formattedStartDate = $startDate ? $startDate->format("d/m/Y") : null;

        // Création d'une nouvelle instance de BorneDto avec les données extraites et formatées
//         return new self(
//             // $chargingPoint->getId(),
//             // $formattedStartDate,
//         );
//     }
// }
